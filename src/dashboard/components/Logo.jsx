import styled from "styled-components";
export const Logo = styled.img`
    animation: spin infinite 30s linear;
    height: 100px;
    display: block;
    margin-left: auto;
    margin-right: auto;
    pointer-events: none;
`;
